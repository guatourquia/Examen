<%= form_with(model: estrella, local: true) do |form| %>
  <% if estrella.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(estrella.errors.count, "error") %> prohibited this estrella from being saved:</h2>

      <ul>
      <% estrella.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Nombre %>
    <%= form.text_field :Nombre, id: :estrella_Nombre %>
  </div>

  <div class="field">
    <%= form.label :Descripcion %>
    <%= form.text_area :Descripcion, id: :estrella_Descripcion %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
