<%= form_with(model: planetum, local: true) do |form| %>
  <% if planetum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(planetum.errors.count, "error") %> prohibited this planetum from being saved:</h2>

      <ul>
      <% planetum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Nombre %>
    <%= form.text_field :Nombre, id: :planetum_Nombre %>
  </div>

  <div class="field">
    <%= form.label :Descripcion %>
    <%= form.text_area :Descripcion, id: :planetum_Descripcion %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
